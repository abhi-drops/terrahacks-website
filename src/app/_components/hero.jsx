import Image from 'next/image';

export default function Hero() {
    return (
        <div id="hero" className="flex justify-start min-h-screen bg-hero-section bg-no-repeat bg-top bg-contain 2xl:bg-center 2xl:bg-cover">
            <div className='flex flex-row'>
            </div>
        </div>
    )
}